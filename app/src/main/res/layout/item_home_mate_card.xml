<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_bookii_mate"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="20dp"
    app:strokeWidth="0dp"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- 프로필 -->
        <ImageView
            android:id="@+id/iv_mate_profile"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_home_profile"
            android:clipToOutline="true"
            android:src="@drawable/img_profile_default"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <LinearLayout
            android:id="@+id/layout_mate_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            app:layout_constraintStart_toEndOf="@id/iv_mate_profile"
            app:layout_constraintTop_toTopOf="@id/iv_mate_profile"
            app:layout_constraintBottom_toTopOf="@id/tv_recent_book"
            android:layout_marginStart="12dp"
            app:layout_constraintEnd_toStartOf="@id/iv_mate_arrow">

            <!-- 이름 -->
            <TextView
                android:id="@+id/tv_mate_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="닉네임"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/grey_900"
                android:gravity="center_vertical" />

            <TextView
                android:id="@+id/tv_tag_1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/BookiiChip"
                android:layout_marginStart="8dp"
                android:text="#마로형" />

            <TextView
                android:id="@+id/tv_tag_2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/BookiiChip"
                android:layout_marginStart="6dp"
                android:text="#인사이트" />
        </LinearLayout>

        <!-- 최근 책 텍스트 -->
        <TextView
            android:id="@+id/tv_recent_book"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="가장 최근에 완독한 책 제목"
            android:textSize="12sp"
            android:textColor="@color/grey_600"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="@id/layout_mate_title"
            app:layout_constraintTop_toBottomOf="@id/layout_mate_title"
            app:layout_constraintEnd_toStartOf="@id/iv_mate_arrow"
            app:layout_constraintBottom_toBottomOf="@id/iv_mate_profile" />

        <!-- 오른쪽 화살표 -->
        <ImageView
            android:id="@+id/iv_mate_arrow"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_right_arrow"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>