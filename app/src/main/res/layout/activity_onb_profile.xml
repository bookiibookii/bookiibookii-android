<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:background="@color/ui_bg"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="18dp"
    android:paddingBottom="20dp"
    tools:context=".onboarding.profile.OnbProfileActivity">

    <!-- TODO: src 원본 @drawable/ic_home_logo -->
    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="204dp"
        android:layout_height="21.79dp"
        android:src="@android:color/transparent"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <FrameLayout
        android:id="@+id/profile_container"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toBottomOf="@id/iv_logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- TODO: background 원본 @drawable/bg_onb_profile -->
        <!-- TODO: src 원본 @drawable/img_profile_default -->
        <ImageView
            android:id="@+id/iv_profile"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:clipToOutline="true"
            android:src="@android:color/transparent"
            android:scaleType="centerCrop" />

        <!-- TODO: src 원본 @drawable/ic_camera_button -->
        <ImageView
            android:id="@+id/iv_profile_edit"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="bottom|end"
            android:src="@android:color/transparent"
            android:clickable="true"
            android:focusable="true" />

    </FrameLayout>

    <!-- TODO: background 원본 @drawable/bg_onb_input_default -->
    <FrameLayout
        android:id="@+id/layout_nickname_row"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="50dp"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toBottomOf="@id/profile_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <EditText
            android:id="@+id/et_nickname"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="닉네임을 입력해주세요"
            android:textSize="15sp"
            android:paddingStart="20dp"
            android:paddingEnd="130dp"
            android:inputType="text"
            android:maxLength="10"
            android:background="@null"
            android:gravity="center_vertical"
            android:textColor="@color/grey_900"
            android:textColorHint="@color/grey_500"/>

        <!-- TODO: background 원본 @drawable/selector_onb_check_button_bg -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_check"
            android:layout_width="100dp"
            android:layout_height="40dp"
            android:layout_marginEnd="5dp"
            android:text="중복확인"
            android:textSize="14sp"
            android:textColor="@color/grey_100"
            android:textAllCaps="false"
            android:enabled="false"
            app:backgroundTint="@null"
            android:layout_gravity="center_vertical|end"
            android:background="@android:color/transparent"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:stateListAnimator="@null" />

    </FrameLayout>

    <!-- TODO: background 원본 @drawable/bg_validation_error -->
    <LinearLayout
        android:id="@+id/layout_validation"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        tools:visibility="visible"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toBottomOf="@id/layout_nickname_row"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- TODO: src 원본 @drawable/ic_error -->
        <ImageView
            android:id="@+id/iv_validation"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@android:color/transparent"
            android:contentDescription="@null"/>

        <TextView
            android:id="@+id/tv_validation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="중복된 닉네임입니다."
            android:textSize="14sp"
            android:letterSpacing="-0.02"
            android:textColor="@color/ui_point_red"/>
    </LinearLayout>

    <include
        android:id="@+id/include_footer_button"
        layout="@layout/include_onb_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>