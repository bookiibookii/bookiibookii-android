<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/splashRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/ui_main">

        <!-- 중앙 로고 + 워드마크 + 태그라인 -->
        <LinearLayout
            android:id="@+id/logoContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_bias="0.38">

            <ImageView
                android:id="@+id/imgLogo"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:contentDescription="@string/app_name"
                android:src="@drawable/ic_logo_symbol" />

            <ImageView
                android:id="@+id/imgTitle"
                android:layout_width="204dp"
                android:layout_height="22dp"
                android:layout_marginTop="20dp"
                android:contentDescription="@string/app_name"
                android:src="@drawable/ic_logo_wordmark" />

            <!-- 태그라인 -->
            <TextView
                android:id="@+id/tv_tagline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="center"
                android:textSize="14sp"
                android:textColor="@color/white"
                android:fontFamily="@font/pretendard_regular"
                android:letterSpacing="-0.01"
                android:lineSpacingMultiplier="1.4" />

        </LinearLayout>

        <!-- SNS 로그인 섹션 (label + 3 버튼) -->
        <LinearLayout
            android:id="@+id/layout_sns_section"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tv_terms_notice"
            android:layout_marginBottom="24dp">

            <!-- SNS 안내 문구 -->
            <TextView
                android:id="@+id/tv_sns_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SNS 계정으로 간편 가입하기"
                android:textSize="14sp"
                android:textColor="@color/white"
                android:fontFamily="@font/pretendard_regular"
                android:letterSpacing="-0.01"
                android:layout_marginBottom="12dp" />

            <!-- 2개 원형 버튼 (AOS: 카카오 + 구글) -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- 카카오 -->
                <ImageButton
                    android:id="@+id/btn_kakao_circle"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:background="@drawable/bg_kakao_circle"
                    android:src="@drawable/ic_kakao"
                    android:padding="22dp"
                    android:contentDescription="@null"
                    android:scaleType="fitCenter" />

                <Space android:layout_width="24dp" android:layout_height="1dp" />

                <!-- 구글 -->
                <ImageButton
                    android:id="@+id/btn_google_circle"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:background="@drawable/bg_white_circle"
                    android:src="@drawable/ic_google"
                    android:padding="22dp"
                    android:contentDescription="@null"
                    android:scaleType="fitCenter" />

            </LinearLayout>

        </LinearLayout>

        <!-- 약관 안내 문구 -->
        <TextView
            android:id="@+id/tv_terms_notice"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="24dp"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:fontFamily="@font/pretendard_regular"
            android:letterSpacing="-0.01"
            android:lineSpacingMultiplier="1.4"
            android:includeFontPadding="false"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- 로그인 성공 문구: SNS 섹션과 같은 수직 위치 (Figma top=702dp/917dp) -->
        <TextView
            android:id="@+id/tv_login_complete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="로그인이 성공적으로 완료되었습니다"
            android:textSize="16sp"
            android:letterSpacing="-0.01"
            android:fontFamily="@font/pretendard_medium"
            android:textColor="@color/white"
            android:lineSpacingMultiplier="1.4"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tv_terms_notice"
            app:layout_constraintVertical_bias="0.86" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
