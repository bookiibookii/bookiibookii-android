<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".onboarding.login.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookiiBookii"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".common.ComErrorActivity"
            android:exported="false" />
        <activity
            android:name=".onboarding.Intro.LoginIntroActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BookiiBookii.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".onboarding.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".home.notification.ui.NotificationActivity"
            android:exported="false" />
        <activity
            android:name=".home.notification.ui.NotificationKeywordSettingActivity"
            android:exported="false" />
        <activity
            android:name=".onboarding.steps.OnbStepActivity"
            android:exported="false" />
        <activity
            android:name=".common.BaseActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="kakao${KAKAO_APP_KEY}" />
            </intent-filter>
        </activity>

        <activity
            android:name=".library.feat.PublicCardViewerActivity"
            android:exported="true"
            android:theme="@style/Theme.BookiiBookii">
            <!-- 독서카드 공유 링크 진입 (App Links). host는 실제 shareUrl 도메인에 맞게 수정 필요 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="bookiibookii.com"
                    android:pathPrefix="/share/reading-card" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>